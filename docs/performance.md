# Performance

The initial baseline is intentionally pending a stable browser reference machine. Reproducible
scenarios live in `benchmarks/`; numerical release thresholds will be recorded only after the
worker-first vertical slice is measured. Required reports distinguish UI rAF FPS from rendered and
presented RLottie FPS and include worker count, dropped frames, render p95, and lifecycle memory.
