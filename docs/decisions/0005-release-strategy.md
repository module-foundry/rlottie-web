# ADR 0005: Changesets release PR

Status: accepted.

Use Changesets to prepare the single root public package version, then publish only from the
protected release workflow using npm OIDC provenance.
